// Update this page (the content is just a fallback if you fail to update the page)

import VirtualTryOnApp from "@/components/VirtualTryOnApp";

const Index = () => {
  return <VirtualTryOnApp />;
};

export default Index;
